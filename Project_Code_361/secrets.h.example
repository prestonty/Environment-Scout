#pragma once

// Copy this file to secrets.h (same folder) and fill in real values.
// secrets.h is gitignored -- it never gets committed.

#define SECRET_AP_PASS    "replace-with-ap-password"     // must be 8+ chars
#define SECRET_STA_SSID   "replace-with-wifi-ssid"
#define SECRET_STA_PASS   "replace-with-wifi-password"
#define SECRET_UPLOAD_KEY "replace-with-your-api-key"
